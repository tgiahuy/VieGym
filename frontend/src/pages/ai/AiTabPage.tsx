import { AppBar } from '@/components/shared/AppBar';

export default function AiTabPage() {
  return (
    <div>
      <AppBar title="AI Coach" />
      <main className="container p-4 mx-auto mt-4">
        <p className="text-muted-foreground">Giao diện Tab AI (MH27) sẽ được triển khai ở bước tiếp theo.</p>
      </main>
    </div>
  );
}